# Mosaic UI Service - Local Configuration Overrides
# This file overrides settings from .env.shared for the mosaic-ui service

# Service-specific settings
SERVICE_NAME=mosaic-ui
DEBUG=true
ENVIRONMENT=development

# UI-specific configuration
UI_TITLE=Mosaic MCP Tool Validation Interface
UI_PAGE_ICON=🎯
SERVER_PORT=8501

# Override shared settings as needed for development
# AZURE_OPENAI_ENDPOINT=your-dev-endpoint
# AZURE_COSMOS_ENDPOINT=your-dev-cosmos-endpoint

# UI service doesn't need heavy processing capabilities
LOG_LEVEL=INFO