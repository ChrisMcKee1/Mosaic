# Mosaic UI Service - Streamlit Validation Tool
# Core Streamlit and UI dependencies
streamlit>=1.28.0

# Shared Mosaic dependencies (lightweight query components only)
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# Optional: Full Mosaic integration (comment out if dependencies unavailable)
# These allow real database querying vs simulated mode
# azure-cosmos>=4.5.0
# azure-identity>=1.15.0
# semantic-kernel>=0.9.0
# azure-ai-openai>=1.0.0