# Ingestion Service - Local Configuration Overrides
# This file overrides settings from .env.shared for the ingestion service

# Service-specific settings
SERVICE_NAME=ingestion_service
DEBUG=true
ENVIRONMENT=development

# Ingestion-specific configuration
SERVER_PORT=8002
LOG_LEVEL=DEBUG

# Heavy processing capabilities
AZURE_USE_MANAGED_IDENTITY=true

# Override shared settings as needed for development
# AZURE_OPENAI_ENDPOINT=your-dev-endpoint
# AZURE_COSMOS_ENDPOINT=your-dev-cosmos-endpoint

# Ingestion service specific environment variables
INGESTION_BATCH_SIZE=100
INGESTION_MAX_WORKERS=4
INGESTION_TIMEOUT_SECONDS=300